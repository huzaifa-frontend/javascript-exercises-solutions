var greeting;
var hour = 13;

if (hour < 18) {
greeting = "Good day";
}
else {
greeting = "Good evening";
}
// The else statement should not be inside the curly braces of the if block. 
// Instead, it should follow the closing curly brace of the if block.